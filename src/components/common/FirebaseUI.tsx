
'use client';

export function FirebaseUI() {
  // This component is no longer used, but is kept to prevent breaking imports.
  // The login logic has been moved to /src/app/login/page.tsx and uses a custom form.
  return null;
}
