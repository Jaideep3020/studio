
export interface Student {
    id: string;
    name: string;
}

export interface Lecture {
    id: string;
    description: string;
}

export interface LecturePayload {
    id: string;
    description: string;
}
